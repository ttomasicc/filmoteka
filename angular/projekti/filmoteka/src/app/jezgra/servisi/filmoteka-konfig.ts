import {environment} from '../../../environments/environment';

export const appDomena: string = `http://localhost:${environment.app.port}/api`;
export const restDomena: string = `http://localhost:${environment.rest.port}/api`;
